/**
 * Created by Sandeep on 01/06/14.
 */
/*angular.module('fairRentApp.filter', [])
.filter('inflationTime', function ($filter) {
    return function (input) {
        if (input == null) { return ""; }
        
        var _date = $filter('date')(input, 'YYYY-MM-dd');
        
        return _date;

    };
});*/